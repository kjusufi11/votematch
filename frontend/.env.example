# Frontend environment variables
# In production: set VITE_API_URL to your backend API URL
# e.g. https://api.votematch.app/api
# Leave blank in dev — Vite proxy handles it automatically

VITE_API_URL=
